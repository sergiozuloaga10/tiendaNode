doctype html
html(lang="es")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Cerrar Sesión
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
  body
    .container.mt-5.text-center
      .card.shadow-lg
        .card-body
          h1.text-danger Cerrar Sesión
          p.text-secondary Estás a punto de salir de tu cuenta. ¿Deseas continuar?
          form(method="POST" action="/")
            input(type="hidden" name="_csrf" value=csrf)
            button.btn.btn-danger.btn-lg.mt-3(type="submit") Cerrar Sesión
          a.btn.btn-secondary.btn-lg.mt-3(href="/trb") Cancelar

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")
